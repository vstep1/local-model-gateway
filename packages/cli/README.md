# local-model-gateway CLI

Install, configure, inspect, and run lightweight local AI workstation management.

```bash
npx local-model-gateway init
npx local-model-gateway doctor
npx local-model-gateway start
```
