{
  "name": "local-model-gateway",
  "version": "0.1.0",
  "description": "CLI for installing, configuring, and running Local Model Gateway.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "local-model-gateway": "dist/src/index.js",
    "local-ai-gateway": "dist/src/index.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/index.ts",
    "test": "tsx --test tests/**/*.test.ts",
    "typecheck": "tsc -p tsconfig.test.json"
  },
  "dependencies": {
    "@local-model-gateway/core": "0.1.0",
    "@local-model-gateway/gateway": "0.1.0",
    "@local-model-gateway/runtime-adapters": "0.1.0",
    "yaml": "^2.8.2"
  }
}
