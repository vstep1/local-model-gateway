{
  "name": "local-model-gateway",
  "version": "0.2.0",
  "description": "CLI for lightweight local AI workstation management.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "local-model-gateway": "dist/src/index.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/index.ts",
    "test": "tsx --test tests/**/*.test.ts",
    "typecheck": "tsc -p tsconfig.test.json"
  },
  "dependencies": {
    "@local-model-gateway/core": "0.2.0",
    "@local-model-gateway/gateway": "0.2.0",
    "@local-model-gateway/runtime-adapters": "0.2.0",
    "yaml": "^2.8.2"
  }
}
