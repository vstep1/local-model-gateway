# @local-model-gateway/gateway

OpenAI-compatible HTTP routes, including managed WAV generation at
`/v1/audio/speech`, plus MCP tools and the browser dashboard for coordinating
local agents, models, and workstation GPU access.
